name: Uptime Ping

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: curl -s -f -o /dev/null "https://voiceflow.ysiddo-ai-projects.app/health" || echo "Ping failed but continuing"
